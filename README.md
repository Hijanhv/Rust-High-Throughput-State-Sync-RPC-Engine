# Rust-High-Throughput-State-Sync-RPC-Engine
A custom async RPC server + concurrent state sync engine that ingests events, maintains deterministic state, and serves queries — designed like real infra, not tutorials. “mini blockchain indexer + state sync layer + RPC node”
